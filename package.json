{
  "name": "game-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter landing dev",
    "dev:reversi": "pnpm --filter reversi dev",
    "dev:blockponjs": "pnpm --filter blockponjs dev",
    "build": "pnpm -r build",
    "test:reversi": "pnpm --filter reversi test"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  }
}
